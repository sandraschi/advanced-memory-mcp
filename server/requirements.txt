# Advanced Memory MCP Server Requirements for DXT Package
# This file lists the dependencies needed to run the Advanced Memory MCP server

# Core FastMCP framework
fastmcp>=0.2.0

# Advanced Memory MCP package (this will be bundled with the DXT)
advanced-memory-mcp>=1.0.0

# Core dependencies
pydantic>=2.0.0
sqlalchemy>=2.0.0
alembic>=1.12.0
httpx>=0.25.0
typer>=0.9.0
rich>=13.0.0
watchfiles>=0.21.0
python-multipart>=0.0.6

# Optional dependencies for enhanced functionality
# These can be installed if needed for specific features
# uv>=0.4.0  # Package manager (should be available in DXT environment)
# pandoc>=3.0.0  # For document export features
# jupyter>=1.0.0  # For notebook integration
