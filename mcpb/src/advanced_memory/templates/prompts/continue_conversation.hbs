# Continuing conversation on: {{topic}}

This is a memory retrieval session. 

Please use the available advanced-memory tools to gather relevant context before responding. Start by executing one of the suggested commands below to retrieve content.

> **Knowledge Capture Recommendation:** As you continue this conversation, actively look for opportunities to record new information, decisions, or insights that emerge. Use `write_note()` to document important context.

Here's what I found from previous conversations:

{{#if has_results}}
{{#each hierarchical_results}}
<memory>
--- memory://{{ primary_result.permalink }}

## {{ primary_result.title }}
- **Type**: {{ primary_result.type }}
- **Created**: {{date primary_result.created_at "%Y-%m-%d %H:%M"}}

{{#if primary_result.content}}
**Excerpt**:
<excerpt>
{{ primary_result.content }}
</excerpt>
{{/if}}

{{#if observations}}
## Observations
{{#each observations}}
<observation>
- [{{ category }}] {{ content }}
</observation>
{{/each}}
{{/if}}

Read this with `adn_content`: operation="read", identifier="{{ primary_result.permalink }}"

{{#if related_results}}
## Related Context

{{#each related_results}}
<related>
- type: **{{ type }}**
- title: {{ title }}

{{#if permalink}}
View with `adn_content`: operation="read", identifier="{{ permalink }}"
{{else}}
View with `adn_content`: operation="read", identifier="{{ file_path }}"
{{/if}}
</related>
{{/each}}
{{/if}}

</memory>
{{/each}}
{{else}}
The supplied query did not return any information specifically on this topic.

## Opportunity to Capture New Knowledge!

This is an excellent chance to start documenting this topic using the `adn_content` tool:
- **operation**: "write"
- **identifier**: "{{ topic }}"
- **folder**: "topics"
- **tags**: "{{ topic }}"
- **content**: Include ## Overview, ## Key Points, ## Observations (with categories), and ## Relations sections

## Other Options

Please use the available advanced-memory tools to gather relevant context before responding.
You can also:
- Try a different search term
- Check recent activity: Use `adn_navigation` with operation="recent_activity" and timeframe="30d"
{{/if}}
## Next Steps
<instructions>
You can:
- Explore more: Use `adn_search` with operation="notes" and query="{{ topic }}"
- See what's changed: Use `adn_navigation` with operation="recent_activity" and timeframe="{{default timeframe "30d"}}"
- **Record new learnings or decisions**: Use `adn_content` with operation="write", a meaningful identifier/title, appropriate folder, and content with observations and relations

## Knowledge Capture Recommendation

As you continue this conversation, **actively look for opportunities to:**
1. Record key information, decisions, or insights that emerge
2. Link new knowledge to existing topics 
3. Suggest capturing important context when appropriate
4. Create forward references to topics that might be created later

Remember that capturing knowledge during conversations is one of the most valuable aspects of Advanced Memory.
</instructions>