# Search Results for: "{{ query }}"{{#if timeframe}} (after {{ timeframe }}){{/if}}

This is a memory search session.
Please use the available advanced-memory tools to gather relevant context before responding.
I found {{ result_count }} result(s) that match your query.

{{#if has_results}}
Here are the most relevant results:

    {{#each results}}
        {{#if_cond (lt @index 5)}}
        {{#dedent}}
            ## {{math @index "+" 1}}. {{ title }}
            - **Type**: {{ type.value }}
            {{#if metadata.created_at}}
            - **Created**: {{date metadata.created_at "%Y-%m-%d %H:%M"}}
            {{/if}}
            - **Relevance Score**: {{round score 2}}

            {{#if content}}
            - **Excerpt**:
            {{ content }}
            {{/if}}

            {{#if permalink}}
            You can view this content with: `adn_content("read", identifier="{{ permalink }}")`
            Or explore its context with: `adn_navigation("build_context", url="memory://{{ permalink }}", depth=2)`
            {{else}}
            You can view this file with: `adn_content("read", identifier="{{ file_path }}")`
            {{/if}}
        {{/dedent}}
        {{/if_cond}}
    {{/each}}

## Next Steps

You can:
- Refine your search: `adn_search("notes", query="{{ query }} AND additional_term")`
- Exclude terms: `adn_search("notes", query="{{ query }} NOT exclude_term")`
- View more results: `adn_search("notes", query="{{ query }}", page=2)`
- Check recent activity: `adn_navigation("recent_activity", timeframe="30d")`

## Synthesize and Capture Knowledge

Consider creating a new note that synthesizes what you've learned:

```python
await adn_content(
    operation="write",
    identifier="Synthesis of {{capitalize query}} Information",
    content='''# Synthesis of {{capitalize query}} Information
    
## Overview
[Synthesis of the search results and your conversation]
    
## Key Insights
[Summary of main points learned from these results]
    
## Observations
- [insight] [Important observation from search results]
- [connection] [How this connects to other topics]
    
## Relations
- relates_to [[{{#if results.length}}{{#if results.0.title}}{{results.0.title}}{{else}}Related Topic{{/if}}{{else}}Related Topic{{/if}}]]
- extends [[Another Relevant Topic]]
''',
    folder="synthesis",
    tags=["search", "synthesis"]
)
```

Remember that capturing synthesized knowledge is one of the most valuable features of Advanced Memory.
{{else}}
    I couldn't find any results for this query.

    ## Opportunity to Capture Knowledge!

    This is an excellent opportunity to create new knowledge on this topic. Consider:

    ```python
    await adn_content(
        operation="write",
        identifier="{{capitalize query}}",
        content='''# {{capitalize query}}

## Overview
[Summary of what we've discussed about {{ query }}]

## Observations
- [category] [First observation about {{ query }}]
- [category] [Second observation about {{ query }}]

## Relations
- relates_to [[Other Relevant Topic]]
''',
        folder="new-topics",
        tags=["{{query}}"]
    )
    ```

    ## Other Suggestions
    - Try a different search term
    - Broaden your search criteria
    - Check recent activity with `adn_navigation("recent_activity", timeframe="30d")`
{{/if}}